services:
  - type: web
    name: agreement-management-backend
    runtime: docker
    rootDir: backend
    plan: free
    dockerfilePath: ./Dockerfile
    preDeployCommand: python manage.py migrate && python create_admin.py
